@{
    ViewBag.Title = "otzivi";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@using System.Web.Optimization
@model eUseControl.Web1.Models.UserFeedb

@using (Html.BeginForm("otzivi", "Product", FormMethod.Post, new { @id = "FeedbackForm" }))
{
    @Html.AntiForgeryToken()
    <!-- BREADCRUMB -->
    <div id="breadcrumb" class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
                <div class="col-md-12">
                    <h3 class="breadcrumb-header">Отзывы</h3>
                    <ul class="breadcrumb-tree">
                        <li><a href="#">Home</a></li>
                        <li class="active">Blank</li>
                    </ul>
                </div>
                @*<textarea name="pojelaniaPolizovatelia" id="123" cols="50" rows="10"
        placeholder="Оставьте свой отзыв"></textarea>*@
                @Html.TextBoxFor(m => m.Text, new { @placeholder = "Напишите Отзыв", @style = "height: 210px;" })
                <input type="submit" class="button" value="Отправить">
            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>
    <!-- /BREADCRUMB -->
    <!-- SECTION -->
    <div class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>
    <!-- /SECTION -->
}